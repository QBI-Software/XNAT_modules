<!DOCTYPE html>
	$page.setTitle("OPEX Virtual Water Maze")
	$page.setLinkColor($ui.alink)
	$page.setVlinkColor($ui.vlink)
##----------------------------------------
## MODIFIED FOR BOOTSTRAP - QBI - e.cooperwilliams@uq.edu.au
##
##----------------------------------------
	#parse("/screens/macrolists.vm")
<script type="text/javascript">
    window.currentID = "$!om.getId()";
		#if($om.getProject())
			#if($om.getProject().equals(""))
            window.currentProject = "$!project";
			#else
            window.currentProject = "$!om.getProject()";
			#end
		#else
        window.currentProject = "$!project";
		#end

    window.currentSubject = "$!om.getSubjectId()";
    window.currentLabel = "$!om.getLabel()";
</script>
#if ($data.message)
<div class="alert alert-danger error"><span>$data.message</span></div>
#end
<div id="EditForm" class="container col-md-8">
    <form ID="form1" name="form1" method="post" action="$link.setAction("ModifySubjectAssessorData")">
        <input type="hidden" name="project" value="$!{project}">
        <input type="hidden" name="part_id" value="$!{part_id}">
        <input type='hidden' name='$om.getXSIType()/ID' value='$!item.getProperty("ID")'/>
        <input type="hidden" name="$om.getXSIType()/project" value='$!{project}'/>
        <input type='hidden' name='$om.getXSIType()/subject_ID'
               value='$!item.getProperty("$om.getXSIType()/subject_ID")'/>
        <input type="hidden" name="$om.getXSIType().date.month" id="$om.getXSIType().date.month" value=""/>
        <input type="hidden" name="$om.getXSIType().date.date" id="$om.getXSIType().date.date" value=""/>
        <input type="hidden" name="$om.getXSIType().date.year" id="$om.getXSIType().date.year" value=""/>
		#if($vr)
            <div class="alert alert-danger">Invalid parameters:<br/>$vr.toHTML()</div>
		#end
        <div class="row btn-success"><h3 class="edit-title">OPEX Virtual Water Maze #if($om.getId()) : Edit #else  : New #end</h3></div>
        <div class="row">
			#parse("/screens/xnat_edit_subjectAssessorData.vm")
        </div>
        <div class="row">
			#parse("/screens/EditProjectSpecificFields.vm")
        </div>
        <!-- BEGIN opex:amunet -->
        <table class="table table-striped">
			<tbody>

			<TR><TD>Interval</TD><TD>#xdatSelectGroup("$om.getXSIType()/interval" "" $item $interval $vr)</TD></TR>
			<TR><TD>AEV Comments</TD><TD>#xdatTextArea("$om.getXSIType()/AEVcomments" $item "" $vr "3" "50")</TD></TR>
			<TR><TD>AEV</TD><TD>#xdatNumberBox("opex:amunet/AEV" $item "" $vr)</TD></TR>
			<TR><TD>EV</TD><TD>#xdatNumberBox("opex:amunet/EV" $item "" $vr)</TD></TR>
			<TR><TD>AV</TD><TD>#xdatNumberBox("opex:amunet/AV" $item "" $vr)</TD></TR>
			<TR><TD>DV</TD><TD>#xdatNumberBox("opex:amunet/DV" $item "" $vr)</TD></TR>
            <TR><TD>SCS Comments</TD><TD>#xdatTextArea("$om.getXSIType()/SCScomments" $item "" $vr "3" "50")</TD></TR>
			<TR><TD>SCS</TD><TD>#xdatNumberBox("opex:amunet/SCS" $item "" $vr)</TD></TR>
			<TR><TD>SCD</TD><TD>#xdatNumberBox("opex:amunet/SCD" $item "" $vr)</TD></TR>
			<TR><TD>SAS</TD><TD>#xdatNumberBox("opex:amunet/SAS" $item "" $vr)</TD></TR>
			<TR><TD>SAD</TD><TD>#xdatNumberBox("opex:amunet/SAD" $item "" $vr)</TD></TR>
			<TR><TD>SES</TD><TD>#xdatNumberBox("opex:amunet/SES" $item "" $vr)</TD></TR>
			<TR><TD>SED</TD><TD>#xdatNumberBox("opex:amunet/SED" $item "" $vr)</TD></TR>
            </tbody>
        </table>

        <!-- END opex:amunet -->
		#parse("screens/validateform.vm")
